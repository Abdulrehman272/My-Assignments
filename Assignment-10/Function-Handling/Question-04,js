// Write a JavaScript program to check whether a number is even or odd using functions.

function isEven(num) {
  return num % 2 === 0;
}

let num = 11;

if (isEven(num)) {
  console.log(`${num} is even`);
} else {
  console.log(`${num} is odd`);
}
